# galaga-on-aks
This project demonstrates the ability to take a stateless application, containerize it, and deploy it on Kubernetes with production-grade characteristics — high availability (no single point of failure) and horizontal scalability (handles variable load automatically).
